const Configuration = async () => {
  return (
    <div className="w-full flex flex-col gap-4 py-20 text-black dark:text-white font-semibold text-lg text-center">
      Configuration not found
    </div>
  );
};

export default Configuration;
